sample
sample2
sampe3

git init

git add .

git commit -m "first text"

git user.name

git log

git config

git config --global user.name

git config --global user.email

//del.git 

-------------------------
git init
git add .
git config user.name "venkatateju"

git config user.email "email"

git commit -m "text"

git log


git add .

git commit -m "second text"  //change the commit name if there is any error

git log

git remote add origin https://github.com/venkatateju/sample.git

git push -u origin master

git log 6acdb73e2 // last one is committed id

git status


